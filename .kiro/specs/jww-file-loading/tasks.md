# Implementation Plan

- [x] 1. 描画データ契約にレイヤーの初期可視状態を導入する
  - 描画データが持つレイヤー一覧を、名前だけの列から「名前 + 初期可視状態」の列へ変更する
  - DXF の変換処理は、収集したレイヤーをすべて可視として返す。並び順の規則は変更しない
  - レイヤー状態の設定処理が可視状態付きの一覧を受け取り、その値を初期状態として保持するようにする。可視判定・表示切替・可視フィルタの挙動と呼び出し方は変更しない
  - ファイル選択処理からの受け渡しを新しい形に合わせ、既存の変換テストとレイヤー関連テストを追随させる
  - 完了状態: `npm run typecheck` と `npm test` が緑のまま、初期非表示で渡したレイヤーが可視判定で非表示になり、表示に切り替えると描画・線分フィルタの対象に戻ることがテストで確認できる
  - _Requirements: 1.3, 3.3, 3.4, 3.5, 3.7_

- [ ] 2. JWW バイナリの解析
- [x] 2.1 MFC アーカイブ形式の読み取りカーソルを実装する
  - バイト位置を保持し、1/2/4 バイト整数・倍精度小数・長さ前置きの文字列・要素数・オブジェクトタグを順に読み出せるようにする
  - 文字列は CP932 としてデコードし、日本語を文字化けなく取り出す
  - 読み取りが範囲外になる場合は、バイト位置を含む解析エラーとして失敗させる
  - 合成したバイト列で読み取り順・値・エラーを検証するユニットテストを追加する
  - 完了状態: 日本語文字列を含む合成バイト列から各値が期待どおり読み出せ、範囲外読み取りがバイト位置付きで失敗することがテストで確認できる
  - _Requirements: 3.6, 5.3_
  - _Boundary: JwwArchiveReader_

- [ ] 2.2 署名・バージョンとレイヤグループ／レイヤ状態ブロックを解析する
  - 先頭の署名とデータバージョンを検証し、署名不一致は解析エラーとして失敗させる
  - ファイルメモ・図面サイズ・書込レイヤグループに続く、16 レイヤグループ × {状態・書込レイヤ・縮尺・プロテクト + 16 レイヤ × {状態・プロテクト}} を読み取る
  - テスト内で合成 JWW バイト列を組み立てるビルダ（ヘッダ全体と図形リストを後続タスクから拡張できる形）をテストファイル内に用意する
  - 完了状態: ビルダで作った合成ファイルから、指定したレイヤグループ／レイヤの状態と縮尺が取り出せ、署名不一致が解析エラーになることがテストで確認できる
  - _Requirements: 3.3, 5.4_
  - _Boundary: jww-parser_
  - _Depends: 2.1_

- [ ] 2.3 ヘッダ後半を走査してレイヤ名を取得し図形リスト先頭に到達する
  - ダミー領域・寸法設定・目盛設定・印刷設定・線色線種定義・文字設定など、状態ブロック以降のヘッダ項目を順に読み飛ばす
  - レイヤ名 256 件とレイヤグループ名 16 件を取り出す
  - バージョンによる項目数の差（マークジャンプと文字描画状態、図形の線幅、拡張線色・線種定義）を分岐として実装する
  - 完了状態: 合成ファイルから日本語・空を含むレイヤ名とレイヤグループ名が正しい対応で取り出せ、ヘッダ走査後の読み取り位置が図形リスト先頭と一致することがテストで確認できる
  - _Requirements: 3.2, 3.6_
  - _Boundary: jww-parser_
  - _Depends: 2.2_

- [ ] 2.4 図形リストのクラスタグを解決して線と円弧を取り出す
  - 要素数を読み、オブジェクトタグから初出クラス名を記録して以降のタグ参照を解決する
  - 線の始終点、円弧の中心・半径・開始角・円弧角・傾き角・扁平率・全円フラグ、および各図形のレイヤアドレスを取り出す
  - ブロック定義リスト以降は読まずに解析を終了する
  - 完了状態: 線と円弧だけを含む合成ファイルから、座標・半径・角度・レイヤアドレスが期待値どおり取り出せることがテストで確認できる
  - _Requirements: 2.1, 2.2_
  - _Boundary: jww-parser_
  - _Depends: 2.3_

- [ ] 2.5 対象外クラスを正確に消費し解析エラーを確定させる
  - 点・文字・寸法・ソリッド・ブロック参照の各クラスについて、条件付きフィールド（点コード付きの点、任意色のソリッド、拡張版の寸法、文字の 2 つの文字列）を含めてフィールドを正確に消費する
  - 消費のみ行った図形の件数を結果に持たせ、線と円弧だけを抽出結果に含める
  - 未知のクラス名・要素数の異常・範囲外参照は、バイト位置を含む解析エラーとして失敗させる（部分結果を返さない）
  - 完了状態: 対象外クラスを線と線の間に挟んだ合成ファイルで後続の線が取りこぼされず、未知クラス名では解析エラーになることがテストで確認できる
  - _Requirements: 2.4, 5.3_
  - _Boundary: jww-parser_
  - _Depends: 2.4_

- [ ] 3. JWW の解析結果を描画命令・線分・レイヤー一覧へ変換する
  - グループ番号とレイヤ番号の 16 進表記を常に先頭に置き、名前があれば併記する規則でレイヤー識別子を生成する。名前未設定でも識別子が成立し、同名の異なるアドレスでも衝突しないようにする
  - 線は描画命令と線分の両方を生成し、円弧は全円フラグで円と円弧に振り分けて描画命令のみを生成する
  - 扁平率が 1 から離れる円弧は変換対象から除外し、同じファイル内の他の図形は変換を続ける
  - 座標は縮尺換算せずそのまま渡し、境界は可視・非可視を問わず変換対象の全図形から求める
  - 変換対象の図形が存在するレイヤーだけを一覧に含め、レイヤ状態が非表示のものを初期非表示とする。描画命令・線分・レイヤー一覧のいずれにも同一の識別子を使う
  - 完了状態: `npm run typecheck` が緑で、解析結果を渡すと線・真円・円弧に対応する描画命令と線分、初期可視状態つきレイヤー一覧、境界が得られる（検証はタスク 5.1 が担う）
  - _Requirements: 2.1, 2.2, 2.3, 2.7, 3.1, 3.2, 3.3, 3.7, 4.4, 4.5_
  - _Boundary: jww-drawing_
  - _Depends: 1, 2.5_

- [ ] 4. 読み込み経路への統合
- [ ] 4.1 ファイル名とバイト列から描画データまたは分類済みエラーを返す読み込み処理を作る
  - 拡張子を大文字小文字の区別なく判定し、DXF は既存のテキスト解析経路へ、JWW は新しい解析経路へ振り分ける
  - 対応外の拡張子は解析せず、対応形式を伝えるメッセージを返す
  - 解析例外を捕捉して形式別のメッセージに変換し、呼び出し側へ例外を漏らさない
  - 描画対象が 0 件の場合は失敗ではなく「対応要素なし」として区別する
  - 画面要素を参照せず、ファイル内容を外部へ送出しない
  - 完了状態: `npm run typecheck` が緑で、成功・対応形式外・対応要素なし・解析失敗の 4 分類とそれぞれの文言が呼び出し側から判別できる（検証はタスク 5.1 が担う）
  - _Requirements: 1.2, 1.3, 1.4, 1.5, 2.5, 5.3, 5.4, 5.6_
  - _Boundary: drawing-loader_
  - _Depends: 3_

- [ ] 4.2 ファイル選択の処理を新しい読み込み経路に接続する
  - ファイル選択時にバイト列を取得して読み込み処理へ渡し、結果の分類に応じて表示を切り替える
  - 読み込み中メッセージ・完了メッセージ（ファイル名・要素数・レイヤー数）の表示は現行の組み立てを維持する
  - 成功時はレイヤー一覧を可視状態つきで配布し、全体が収まる初期表示に合わせる
  - 失敗時は図面表示領域を空にし、拡大・縮小・全体表示を無効状態に戻す
  - 新しいファイルの読み込み時に、直前の測定・エッジ選択・レイヤー表示設定を破棄する現行の挙動を保つ
  - ファイル選択の受け付け対象に JWW を加える
  - 完了状態: 開発サーバー上で JWW を選ぶと図面が描画されてレイヤー一覧に初期可視状態が反映され、解析失敗時は表示が空になり操作が無効化される
  - _Requirements: 1.1, 2.6, 3.4, 3.5, 4.1, 4.6, 5.1, 5.2, 5.5_
  - _Depends: 4.1_

- [ ] 5. 検証
- [ ] 5.1 (P) 読み込みから描画データ化までの統合テストを追加する
  - このタスクが読み込み経路のテストファイルを単独で所有する（タスク 3 と 4.1 は実装のみを担う）
  - 拡張子判定（大文字を含む）と対応外拡張子の扱いを検証する
  - JWW の線から描画命令と線分が、円弧から描画命令のみが生成されることを検証する
  - 扁平な円弧の除外と、同一ファイル内の他図形が残ることを検証する
  - レイヤー識別子が名前あり・名前なし・同名異アドレスで一意かつ規則どおりになることを検証する
  - 非表示レイヤーが初期非表示として渡ること、全レイヤー非表示でも一覧が返ることを検証する
  - 対応要素なし・解析失敗の分類と文言、DXF 経路の従来どおりの読み込みを検証する
  - 完了状態: 上記の観点が `npm test` で全て緑になる
  - _Requirements: 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 2.5, 3.1, 3.2, 3.3, 3.7, 4.4, 4.5, 5.3_
  - _Boundary: drawing-loader.test_
  - _Depends: 4.1_

- [ ] 5.2 実ファイルで読み込みと既存機能の回帰を確認する
  - 前提: 検証用の実 JWW ファイルは利用者提供の 3 件（`private/` 配下、いずれもデータバージョン 700 の建築平面図）。`private/` は Git 管理外であり、テストフィクスチャとしてリポジトリに取り込まない
  - 実 JWW ファイルを開き、図形の欠落や崩れがないことを確認する
  - 日本語レイヤー名の表示と、作図側で非表示にしたレイヤーが初期非表示になることを確認する
  - 頂点・交点のスナップ測定、距離と ΔX / ΔY の表示、エッジ選択とレイヤー名表示、ビュー操作を確認する
  - 既存の DXF ファイルが従来どおり開けることを確認する
  - バージョン 700 が「420 以降」の分岐で正しく読めるという仮説が成立したかを記録する（不成立ならヘッダ走査の追加分岐が必要）
  - 完了状態: 使用した実ファイル名とバージョン、上記の確認結果が記録され、未達がない
  - _Requirements: 2.1, 2.2, 3.6, 4.1, 4.2, 4.3, 4.4, 5.6_
  - _Depends: 4.2_

- [ ] 5.3 構成規約と対応形式の記載を実装に合わせる
  - 読み取りカーソルという新しいファイル役割をステアリングの構成規約に追記する（設計で記録が必要と明示した逸脱）
  - 利用者向けの説明に対応形式と JWW の制限（描画対象・スコープ外の要素）を追記する
  - 完了状態: 構成規約に新規ファイル群すべての役割が記載され、説明の対応形式が DXF と JWW の両方になっている
  - _Depends: 4.2_

## Implementation Notes

- MFC オブジェクトタグの実定義は `wNullTag = 0x0000` / `wNewClassTag = 0xFFFF` / `wClassTag = 0x8000` / `wBigObjectTag = 0x7FFF`。クラス参照のインデックスは `tag & 0x7FFF`。MS TN002・MFC `arcobj.cpp`・LibreCAD jwwlib の 3 源で確認済み（タスク 2.1）
- `wBigObjectTag`（`0x7FFF`）は design.md の `JwwObjectTag` が 3 種のみのため未対応。後続 DWORD を消費しないが、未知クラスインデックスは jww-parser が解析エラーにするため無音では壊れない（タスク 2.1 レビュー指摘）
- 新規クラスタグ内のクラス名は `CString`（CP932）ではなく `WORD` スキーマ + `WORD` 長 + 生 ASCII（`CRuntimeClass::Store` の形式）
- MFC `CString` の長さ前置きは BYTE（`0xFF` 未満）→ WORD（`0xFFFF` 未満）→ DWORD のエスケープ連鎖。Unicode マーカ `0xFFFE` は JWW（ANSI/CP932）では出現しないため未対応
- MFC のカウントは WORD、`0xFFFF` がエスケープで DWORD が続く
- `JwwParseError` は依存方向を一方向に保つため `jww-archive-reader.ts` に定義。`jww-parser.ts` 側から re-export して design.md の公開インターフェースを維持する
- `TextDecoder("shift_jis")` は Vitest（Node v24.19.0）で正常動作を確認済み。research.md のリスク項目はクローズ
